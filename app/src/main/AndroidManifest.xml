<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ashish.alumini">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:name=".application.MyApplication"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.PreLogin.ActivitySplashScreen"
            android:label="Alumni">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".profile" />
        <activity android:name=".Edit_Profile" />


        <activity android:name=".activities.PreLogin.Login"/>
        <activity android:name=".activities.PreLogin.SignUp"/>
        <activity android:name=".activities.PostLogin.MainScreenActivity"/>
        <activity android:name=".activities.PostLogin.PostLoginActivity"/>
        <activity android:name=".activities.PreLogin.MainActivity"/>
    </application>

</manifest>








